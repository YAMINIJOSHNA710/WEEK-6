function Greeting() {
  const currentHour = new Date().getHours();
  const greetingMessage = currentHour < 12 ? "Good morning!" : "Good evening!";
  
  return (
    <div>
      <h2>{greetingMessage}</h2>
    </div>
  );
}
export default Greeting;
